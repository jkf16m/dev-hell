def bound_check(string: str, lower_bound: int, upper_bound: int):
    """
    Checks if the passed string respects the specified length limits

    Parameters:
        string(str):
            The string to be checked
        lower_bound(int):
            The lower limit, it can't be less than zero.
        upper_bound(int):
            The upper limit of the length
        
    Returns:
        bool: It's true if the length is the expected. False if the
        length does not respect the limits specified 
    """
    pass